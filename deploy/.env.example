# Copy to .env.production (ignored by Git) and chmod 600.
RELEASE_TAG=
ADMIN_ORIGIN=https://admin-test.metaframer.net
STOREFRONT_ORIGIN=https://catalog-test.metaframer.net
# Independent random secrets, minimum 32 characters each.
SESSION_SECRET=
CATALOG_SHARED_SECRET=
# api_key:api_secret for the scoped catalog reader, never an Administrator token.
FRAPPE_CATALOG_TOKEN=
