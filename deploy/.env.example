# Copy to .env.production (ignored by Git), chmod 600. API-only deployment.
RELEASE_TAG=
# Independent random secrets, minimum 32 characters each.
SESSION_SECRET=
CATALOG_SHARED_SECRET=
# Scoped read-only api_key:api_secret. Never Administrator or a VITE_ variable.
FRAPPE_CATALOG_TOKEN=
